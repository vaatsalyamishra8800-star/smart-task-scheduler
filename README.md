# smart-task-scheduler
Here's a tightened version — 350 characters exactly-ish, ready to paste directly into LinkedIn's description field:  Built a CLI task manager in Python that schedules tasks by priority &amp; deadline. Used a min-heap for O(log n) task retrieval, a hash map for O(1) lookups, and a stack-based undo feature. Added JSON persistence across sessions
